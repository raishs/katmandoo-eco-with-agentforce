/**
 * Uploads an image to Salesforce CDN and sends it as a message
 * @param {string} conversationId - The conversation ID
 * @param {File} file - The image file to upload
 * @param {string} messageId - Unique message ID
 * @returns {Promise} - Resolves when the image is uploaded and sent
 */
// export async function sendImageMessage(conversationId, file, messageId) { ... }

// Remove the image upload related code
// const MAX_FILE_SIZE = 5 * 1024 * 1024; // 5MB
// const ALLOWED_FILE_TYPES = ['image/jpeg', 'image/png', 'image/gif'];

// Remove the sendImageMessage function
// export async function sendImageMessage(conversationId, file, messageId) { ... }

// ... existing code ... 